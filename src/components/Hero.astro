<section id="hero" class="hero">
  <picture>
    <source srcset="/assets/images/hero-small.jpg" media="(max-width: 520px)" />
    <img class="image" src="/assets/images/hero.jpg" alt="hero image" />
  </picture>

  <!-- <img class="image" src="/assets/images/hero.jpg" alt="hero image" /> -->
  <div class="desc">
    <span class="text">Психологическая поддержка людей с сахарным диабетом</span
    >
  </div>
</section>

<style>
  .hero {
    width: 100%;
    position: relative;
    margin-bottom: 120px;
  }
  .image {
    object-fit: cover;
  }
  .desc {
    width: 35%;
    position: absolute;
    top: 140px;
    right: 150px;
    display: flex;
    flex-direction: column;
    background-color: var(--color-bg-third);
    padding: 120px 40px 300px 40px;
  }
  .text {
    font-size: var(--font-size-xl);
    color: #ffffff;
  }

  @media (max-width: 1520px) {
    .hero {
      margin-bottom: 160px;
    }
    .desc {
      width: 80%;
      top: 600px;
      left: 50%;
      transform: translate(-50%, 0);
      text-align: center;
      padding: 40px;
    }
  }

  @media (max-width: 880px) {
    .hero {
      margin-bottom: 120px;
    }
    .image {
      transform: translate(-10%, 0);
    }
  }

  @media (max-width: 620px) {
    .image {
      transform: translate(-15%, 0);
    }
  }

  @media (max-width: 520px) {
    .hero {
      display: flex;
      justify-content: center;
    }
    picture {
      width: 100%;
    }
    .image {
      width: 100%;
      transform: translate(0, 0);
    }
    .desc {
      top: 90%;
      padding: 20px;
    }
  }
</style>
